#include<stdio.h>
int main()
{
 int a[5]={23,10,56,8,90};
 printf("the entered number is:");
 int i;
 int greatest=a[0];
 int smallest=a[0];
 for(i=0;i<=4;i++)
 {
 if(a[i]>biggest}
 greatest=a[i];
 }
 else if(a[i]<smallest)
 {
  smallest=a[i];
  }
  }
  printf("the smallest number in the array is %d",smallest);
  printf("the greast number in the array is %d",greatest);
  return 0;
  ]
  }
