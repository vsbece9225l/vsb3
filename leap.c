#include<stdio.h>
int main()
{
 int year;
 printf("enter the year");
 scanf("%d",&year);
 if(year%100==0)
 {
 else if(year%==400)
 {
  printf("the given year is leap year%d",year);
  }
  else
  {
  printf("the given year is not leap year%d",year);
  }
  return 0;
  }
